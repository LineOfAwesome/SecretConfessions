define( function ( require ) {
	'use strict';

	var App = require( 'App' );

	App.module( 'Modal.Views', function ( Views ) {

		Views.PageLayout = require( 'apps/modal/views/ModalPageLayoutView' );

	} );

} );
